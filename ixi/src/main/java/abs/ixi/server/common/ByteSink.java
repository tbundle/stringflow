package abs.ixi.server.common;

/**
 * A contract for anything to act as {@link ByteSink}; it could be a file,
 * memory bytes or an entity. The interface enforces ability to absorbe bytes
 * from external entities such as {@link ByteSource}
 * 
 * @author Yogi
 *
 */
public interface ByteSink {
    
}
