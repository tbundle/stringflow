package abs.ixi.server;

import abs.ixi.server.router.PacketRouter;

/**
 * A server component which generates packets; as packets are routed by Router,
 * all the implementations must have a {@link PacketRouter} instance.
 * 
 * @author Yogi
 *
 */
public interface PacketProducer {

}
